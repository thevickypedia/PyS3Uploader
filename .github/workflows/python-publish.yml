name: pypi-publish

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "**/__init__.py"

jobs:
  pypi-publisher:
    runs-on: thevickypedia-default
    steps:
      - uses: thevickypedia/pypi-publisher@v3
        env:
          token: ${{ secrets.PYPI_TOKEN }}
